# Gemini access is provided automatically by Netlify AI Gateway in deployed
# environments — no API key needed. For local development with `netlify dev`,
# AI Gateway env vars are injected after the site has at least one production
# deploy. To override locally, set GEMINI_API_KEY here.
# GEMINI_API_KEY=
